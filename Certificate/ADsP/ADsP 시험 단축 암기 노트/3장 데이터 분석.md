운영 목적 데이터는 업무 처리와 서비스 제공을 위해 수집되는 데이터이다.

분석 목적 데이터는 의사결정, 예측, 패턴 발견을 위해 수집·가공되는 데이터이다.

운영 목적 데이터는 현재 업무를 정확하게 처리하는 것이 중요하고,
분석 목적 데이터는 과거 데이터를 바탕으로 의미 있는 인사이트를 찾는 것이 중요하다.

알고리즘은 문제 해결을 위한 단계적 절차이다.

인공지능은 인간의 지능적 행동을 컴퓨터가 수행하도록 하는 기술이다.

머신러닝은 데이터를 통해 패턴을 학습하여 예측이나 분류를 수행하는 기술이다.

데이터 마이닝은 대량의 데이터에서 의미 있는 패턴, 규칙, 관계를 발견하는 과정이다.

머신러닝은 학습과 예측에 초점이 있고,
데이터 마이닝은 숨겨진 패턴과 규칙 발견에 초점이 있다.

데이터 처리는 원천 데이터를 분석 가능한 형태로 정제, 변환, 결합하는 과정이다.

필터링은 조건에 맞는 데이터를 선택하는 작업이다.

정렬은 데이터를 특정 기준에 따라 순서대로 배열하는 작업이다.

집계는 데이터를 그룹별로 묶어 합계, 평균, 개수 등을 계산하는 작업이다.

데이터 변형은 데이터의 형태나 표현 방식을 분석 목적에 맞게 바꾸는 작업이다.

정규화는 값의 범위를 일정하게 맞추는 것이고,
표준화는 평균 0, 표준편차 1이 되도록 변환하는 것이다.

파생변수는 기존 변수를 가공하거나 조합하여 새롭게 만든 변수이다.

데이터 결합은 여러 테이블이나 데이터셋을 분석 목적에 맞게 합치는 작업이다.

데이터 정제는 오류, 중복, 결측값, 이상값 등을 처리하여 데이터 품질을 높이는 작업이다.

결측값은 데이터가 있어야 할 자리에 값이 없는 상태이다.

이상값은 일반적인 데이터 범위에서 크게 벗어난 값이다.

기술 통계는 현재 수집된 데이터를 요약하고 설명하는 통계이다.

추론 통계는 표본 데이터를 이용해 모집단의 특성을 추정하거나 가설을 검정하는 통계이다.

탐색적 데이터 분석은 본격적인 분석 전에 데이터의 구조, 분포, 결측값, 이상값, 변수 관계를 탐색하는 과정이다.

지도학습은 정답이 있는 데이터를 학습하여 새로운 데이터의 결과를 예측하는 방법이다.

비지도학습은 정답 없이 데이터의 숨겨진 구조와 패턴을 찾는 방법이다.

회귀는 연속형 수치 값을 예측하는 지도학습 방법이다.

분류는 미리 정해진 범주를 예측하는 지도학습 방법이다.

정형 데이터 분석은 행과 열로 구성된 표 형태 데이터를 분석하는 것이다.

비정형 데이터 분석은 텍스트, 이미지, 음성, 영상처럼 고정된 구조가 없는 데이터를 분석하는 것이다.

기술 분석은 현재 상태를 설명하는 분석이다.

진단 분석은 원인을 찾는 분석이다.

예측 분석은 미래를 예측하는 분석이다.

처방 분석은 최적의 행동을 제안하는 분석이다.

데이터는 관찰, 측정, 조사, 실험 등을 통해 수집된 값이다.

확률변수는 확률적 실험의 결과를 숫자로 표현한 변수이다.

모집단은 분석자가 알고자 하는 전체 대상이다.

표본은 모집단에서 추출한 일부 대상이다.

모수는 모집단의 특성을 나타내는 값이다.

통계량은 표본의 특성을 나타내는 값이다.

전수조사는 모집단 전체를 조사하는 방법이다.

표본조사는 모집단 일부를 조사하여 전체를 추정하는 방법이다.

단순 랜덤 추출은 모집단의 모든 대상이 같은 확률로 선택되는 표본 추출 방법이다.

계통 추출법은 모집단 목록에서 일정한 간격으로 표본을 추출하는 방법이다.

집락 추출법은 모집단을 여러 집단으로 나눈 뒤 일부 집단을 선택하여 조사하는 방법이다.

층화 추출법은 모집단을 특성이 비슷한 층으로 나눈 뒤 각 층에서 표본을 추출하는 방법이다.

측정은 대상의 속성을 일정한 기준에 따라 숫자나 기호로 표현하는 과정이다.

척도는 측정된 값의 의미와 정보 수준을 나타내는 기준이다.

명목척도는 단순히 대상을 구분하기 위한 척도이다.

서열척도는 순서가 있는 척도이다.

등간척도는 값의 차이가 의미 있는 척도이다.

비율척도는 절대 0이 존재하고 비율 비교가 가능한 척도이다.

질적 데이터는 명목척도와 서열척도에 해당하고,
양적 데이터는 등간척도와 비율척도에 해당한다.

단변량 데이터는 하나의 변수를 분석하는 데이터이다.

다변량 데이터는 여러 변수를 함께 분석하는 데이터이다.

단순은 독립변수가 하나인 경우이고,
다중은 독립변수가 여러 개인 경우이다.

sample은 비교하는 표본이나 집단의 수를 의미하고,
way는 분석에 사용하는 요인의 수를 의미한다.

데이터셋에서 행은 관측치이고,
열은 변수이다.

확률은 어떤 사건이 발생할 가능성을 0과 1 사이의 값으로 나타낸 것이다.

조건부 확률은 어떤 사건이 발생했다는 조건에서 다른 사건이 발생할 확률이다.

독립사건은 한 사건의 발생이 다른 사건의 발생 확률에 영향을 주지 않는 사건이다.

배반사건은 두 사건이 동시에 발생할 수 없는 사건이다.

확률분포는 확률변수가 가질 수 있는 값과 그 값이 발생할 확률을 나타낸 것이다.

이산형 확률분포는 셀 수 있는 값을 가지는 확률변수의 분포이다.

연속형 확률분포는 연속적인 값을 가지는 확률변수의 분포이다.

결합 확률분포는 두 개 이상의 확률변수가 동시에 가질 수 있는 값과 확률을 나타낸 분포이다.

경험적 분포는 실제 관측 데이터를 바탕으로 만든 분포이다.

이론적 분포는 수학적 확률 모형에 의해 정의된 분포이다.

베르누이 분포는 결과가 성공 또는 실패처럼 두 가지인 시행의 확률분포이다.

이항 분포는 동일한 베르누이 시행을 여러 번 반복했을 때 성공 횟수의 분포이다.

포아송 분포는 일정한 시간이나 공간에서 사건이 발생하는 횟수의 분포이다.

균일 분포는 특정 구간 안에서 모든 값이 동일한 확률로 발생하는 분포이다.

정규 분포는 평균을 중심으로 좌우 대칭인 종 모양의 분포이다.

표준 정규 분포는 평균이 0이고 표준편차가 1인 정규 분포이다.

지수 분포는 어떤 사건이 발생할 때까지 걸리는 시간이나 간격을 나타내는 분포이다.

t 분포는 표본 수가 작고 모집단 분산을 모를 때 평균 검정에 사용된다.

카이제곱 분포는 범주형 데이터의 빈도 차이 검정에 사용된다.

F 분포는 두 분산의 비율이나 여러 집단 평균 비교에 사용된다.

기댓값은 확률변수가 평균적으로 가질 것으로 기대되는 값이다.

분산은 데이터가 평균으로부터 얼마나 떨어져 있는지를 나타내는 지표이다.

표준편차는 분산의 제곱근이며, 데이터의 흩어짐을 원래 단위로 나타낸다.

왜도는 데이터 분포의 비대칭 정도를 나타낸다.

첨도는 데이터 분포가 얼마나 뾰족하거나 꼬리가 두꺼운지를 나타낸다.

분위수는 데이터를 크기순으로 정렬했을 때 특정 위치에 해당하는 값이다.

공분산은 두 변수가 함께 변하는 방향을 나타내는 값이다.

상관계수는 두 변수의 선형 관계의 방향과 강도를 나타내는 표준화된 지표이다.

기술통계량은 수집된 데이터를 요약하고 설명하기 위한 통계값이다.

순서 통계량은 데이터를 정렬했을 때 위치에 따라 정해지는 통계량이다.

최솟값은 데이터 중 가장 작은 값이다.

최댓값은 데이터 중 가장 큰 값이다.

중앙값은 데이터를 크기순으로 정렬했을 때 가운데 위치한 값이다.

사분위수는 데이터를 4등분하는 위치의 값이다.

IQR은 제3사분위수에서 제1사분위수를 뺀 값이다.

십분위수는 데이터를 10등분하는 값이다.

백분위수는 데이터를 100등분하는 값이다.

중심 척도는 데이터의 대표값을 나타내는 지표이다.

평균은 모든 값을 더한 뒤 데이터 개수로 나눈 값이다.

중앙값은 정렬된 데이터의 가운데 값이며,
이상값의 영향을 평균보다 덜 받는다.

최빈값은 데이터에서 가장 자주 나타나는 값이다.

산포 척도는 데이터가 중심으로부터 얼마나 퍼져 있는지를 나타내는 지표이다.

범위는 최댓값에서 최솟값을 뺀 값이다.

사분위 범위는 Q3에서 Q1을 뺀 값이며,
중앙 50% 데이터의 퍼짐 정도를 나타낸다.

정규화는 변수의 범위나 단위를 조정하여 서로 비교할 수 있도록 만드는 과정이다.

최소-최대 정규화는 데이터를 0과 1 사이의 값으로 변환하는 방법이다.

중심화는 각 데이터에서 평균을 빼서 평균이 0이 되도록 만드는 방법이다.

척도화는 변수의 단위나 크기를 조정하는 과정이다.

추정은 표본에서 계산한 통계량을 이용해 모집단의 모수를 추측하는 과정이다.

점 추정은 모수를 하나의 값으로 추정하는 방법이다.

구간 추정은 모수가 포함될 것으로 예상되는 범위를 제시하는 방법이다.

표준오차는 표본 통계량의 변동성을 나타내는 값이다.

통계적 가설 검정은 표본 데이터를 이용해 모집단에 대한 가설의 타당성을 판단하는 절차이다.

귀무가설은 차이가 없다, 효과가 없다, 관계가 없다는 기본 가설이다.

대립가설은 차이가 있다, 효과가 있다, 관계가 있다는 연구 가설이다.

p-value는 귀무가설이 참이라고 가정했을 때 현재 관측된 결과 이상으로 극단적인 결과가 나올 확률이다.

유의수준은 귀무가설을 기각할 기준이 되는 확률이다.

p-value가 유의수준보다 작으면 귀무가설을 기각한다.

검정 통계량은 표본 데이터로부터 계산된 가설 검정의 판단 기준값이다.

1종 오류는 참인 귀무가설을 잘못 기각하는 오류이다.

2종 오류는 거짓인 귀무가설을 기각하지 못하는 오류이다.

t-검정은 평균 차이를 검정하는 방법이다.

일표본 t-검정은 하나의 표본 평균이 특정 기준값과 차이가 있는지 검정하는 방법이다.

독립표본 t-검정은 서로 독립인 두 집단의 평균 차이를 검정하는 방법이다.

대응표본 t-검정은 같은 대상에서 두 번 측정한 값의 평균 차이를 검정하는 방법이다.

상관분석은 두 변수 사이의 선형 관계가 있는지 확인하는 분석이다.

카이제곱 검정은 범주형 데이터의 빈도 차이를 검정하는 방법이다.

적합도 검정은 관측 빈도가 기대 빈도와 일치하는지 검정하는 방법이다.

독립성 검정은 두 범주형 변수가 서로 독립인지 검정하는 방법이다.

동질성 검정은 여러 집단의 범주 분포가 같은지 검정하는 방법이다.

분산분석은 세 개 이상의 집단 평균 차이를 검정하는 방법이다.

비모수 검정은 모집단 분포에 대한 강한 가정 없이 수행하는 검정이다.

윌콕슨 부호순위 검정은 대응표본 t-검정의 비모수 대안이다.

윌콕슨 순위합 검정 또는 맨-휘트니 U 검정은 독립표본 t-검정의 비모수 대안이다.

크루스칼-왈리스 검정은 일원분산분석의 비모수 대안이다.

프리드먼 검정은 반복측정 분산분석의 비모수 대안이다.

평가 지표는 모델의 예측 성능을 수치로 판단하는 기준이다.

회귀 문제의 평가 지표는 실제값과 예측값의 차이를 기준으로 한다.

분류 문제의 평가 지표는 실제 클래스와 예측 클래스의 일치 여부를 기준으로 한다.

MAE는 실제값과 예측값 차이의 절댓값 평균이다.

MAPE는 절대 오차를 실제값으로 나누어 백분율로 표현한 지표이다.

MSE는 실제값과 예측값 차이를 제곱한 값의 평균이다.

RMSE는 MSE에 제곱근을 취한 값이다.

R²는 회귀 모델이 종속변수의 변동을 얼마나 잘 설명하는지 나타내는 지표이다.

오차 행렬은 실제값과 예측값을 비교하여 TP, TN, FP, FN으로 분류 결과를 정리한 표이다.

정확도는 전체 예측 중 맞게 예측한 비율이다.

오분류율은 전체 예측 중 잘못 예측한 비율이다.

재현율은 실제 Positive 중 Positive로 맞게 찾아낸 비율이다.

정밀도는 Positive라고 예측한 것 중 실제 Positive인 비율이다.

F1 Score는 정밀도와 재현율의 조화평균이다.

FPR은 실제 Negative 중 Positive로 잘못 예측한 비율이다.

ROC 곡선은 기준값 변화에 따른 TPR과 FPR의 관계를 나타낸 그래프이다.

AUC가 1에 가까울수록 분류 성능이 좋다.

군집화 평가는 정답 라벨이 없으므로 군집 내 응집도와 군집 간 분리도를 기준으로 한다.

실루엣 계수는 군집 내 응집도와 군집 간 분리도를 함께 평가하는 지표이다.

교차 검증은 데이터를 여러 방식으로 나누어 모델의 일반화 성능을 평가하는 방법이다.

홀드아웃 검증은 데이터를 학습용과 검증용으로 한 번 나누어 평가하는 방법이다.

K-폴드 교차 검증은 데이터를 K개로 나누어 K번 학습과 검증을 반복하는 방법이다.

층화 K-폴드 교차 검증은 클래스 비율을 유지하면서 K개의 폴드로 나누는 방법이다.

LpOCV는 p개의 데이터를 검증용으로 남기고 나머지 데이터로 학습하는 교차 검증 방법이다.

선형회귀는 독립변수와 종속변수 사이의 선형 관계를 이용해 연속형 값을 예측하는 방법이다.

단순 선형회귀는 독립변수 1개로 종속변수를 예측하고,
다중 선형회귀는 여러 개의 독립변수로 종속변수를 예측한다.

선형회귀의 주요 가정은 선형성, 독립성, 정규성, 등분산성이다.

다항 회귀는 변수의 거듭제곱 항을 추가하여 곡선 관계를 표현하는 회귀분석이다.

비선형회귀는 변수와 결과의 관계를 비선형 함수로 표현하는 회귀분석이다.

변수 선택은 모델 성능과 해석력을 높이기 위해 중요한 변수를 선택하는 과정이다.

전진 선택은 변수를 하나씩 추가하고,
후진 제거는 모든 변수에서 시작해 불필요한 변수를 제거하며,
단계적 선택은 추가와 제거를 반복한다.

정규화 선형회귀는 회귀계수에 제약을 주어 과적합을 방지하는 회귀 방법이다.

릿지 회귀는 L2 정규화를 사용하여 계수를 작게 줄인다.

라쏘 회귀는 L1 정규화를 사용하여 일부 계수를 0으로 만들 수 있다.

엘라스틱넷은 L1 정규화와 L2 정규화를 함께 사용하는 회귀 방법이다.

로지스틱회귀는 사건 발생 확률을 예측하여 범주를 분류하는 방법이다.

오즈는 사건이 발생할 확률과 발생하지 않을 확률의 비율이다.

로짓은 오즈에 자연로그를 취한 값이다.

나이브 베이즈는 변수 간 독립을 가정하고 베이즈 정리를 이용해 분류하는 방법이다.

SVM은 클래스 간 마진이 최대가 되는 결정 경계를 찾는 분류 알고리즘이다.

K-NN은 새로운 데이터와 가장 가까운 K개의 이웃을 기준으로 예측하는 방법이다.

의사결정나무는 조건에 따라 데이터를 분할하여 예측하는 나무 구조 모델이다.

회귀 나무는 목표 변수가 연속형일 때 사용하는 의사결정나무이다.

분류 나무는 목표 변수가 범주형일 때 사용하는 의사결정나무이다.

회귀 나무는 분산 감소를 기준으로 분할한다.

분류 나무는 지니 불순도나 엔트로피를 기준으로 분할한다.

가지치기는 불필요한 가지를 제거하여 의사결정나무의 과적합을 줄이는 방법이다.

앙상블은 여러 모델을 결합하여 예측 성능을 높이는 방법이다.

배깅은 여러 모델을 병렬로 학습하고 결과를 결합하여 분산을 줄이는 방법이다.

랜덤 포레스트는 여러 의사결정나무를 결합한 대표적인 배깅 기반 앙상블 모델이다.

부스팅은 이전 모델의 오류를 보완하며 여러 모델을 순차적으로 학습하는 방법이다.

스태킹은 여러 모델의 예측 결과를 다시 새로운 모델의 입력으로 사용해 최종 예측을 수행하는 방법이다.

인공신경망은 입력층, 은닉층, 출력층을 통해 복잡한 패턴을 학습하는 모델이다.

활성 함수는 뉴런의 출력값을 결정하고 신경망이 비선형 관계를 학습할 수 있도록 하는 함수이다.

군집 분석은 유사한 데이터끼리 그룹으로 묶는 비지도학습 방법이다.

K-means는 데이터를 K개의 군집으로 나누고 중심점과의 거리를 기준으로 군집을 반복 갱신하는 방법이다.

계층적 군집분석은 데이터 간 거리를 기준으로 계층 구조를 만들어 군집을 형성하는 방법이다.

연관 분석은 함께 발생하는 항목 간의 관계를 찾는 분석 방법이다.

지지도는 전체 거래 중 특정 항목들이 함께 발생한 비율이다.

신뢰도는 A가 발생한 거래 중 B도 함께 발생한 비율이다.

향상도는 A가 B의 발생 가능성을 얼마나 높이는지 나타내는 지표이다.

Apriori 알고리즘은 빈번 항목 집합을 찾아 연관 규칙을 생성하는 알고리즘이다.