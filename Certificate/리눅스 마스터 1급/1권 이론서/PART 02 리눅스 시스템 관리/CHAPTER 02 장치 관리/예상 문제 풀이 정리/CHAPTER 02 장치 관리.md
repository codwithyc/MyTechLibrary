## 문제 1번 : 정답 1

```

다음 중 리눅스 커널에 대한 설명으로 틀린 것은?

1. 리눅스는 시스템 자원의 관리 역할을 프로그램에게 위임한다.
2. 리눅스 커널 버전 확인은 uname -r 명령으로 할 수 있다.
3. 리눅스 커널은 HAL(Hardware Abstract Layer)를 이용하여 하드웨어 자원을 간접적으로 접근할 수 있도록 한다.
4. 커널 컴파일은 커널 소스를 이용하여 시스템에 최적화된 커널을 만드는 과정이다.

```

## 문제 2번 : 정답 4

```

다음 중 셸에서 example.txt 파일을 프린터로 출력하는 명령으로 알맞은 것은?

1. cat example.txt < /dev/lp0
2. lpstat -p example.txt
3. pr example.txt < printer
4. lp -d /dev/lp0 example.txt

```

## 문제 3번 : 정답 4

```

다음 중 프린트 관련 명령어에 대한 설명으로 틀린 것은?

1. 리눅스에서 프린트 관련 명령어는 BSD 계열과 System V 계열로 나눌 수 있다.
2. BSD 계열 명령어에는 lpr, lprm, lpq 등이 있다.
3. System V 계열 명령어에는 lp, lpstat, cancel 등이 있다.
4. 대부분의 배포판에서는 BSD 계열보다 System V 계열 명령어를 지원한다.

```

## 문제 4번 : 정답 4

```

다음에서 설명하는 명령으로 알맞은 것은?

커널 컴파일 시 설정된 작업을 초기화하기 위해 사용한다. 오브젝트 파일뿐 아니라 설정된 환경 파일인 .config 파일까지 지우며 백업 및 배치 파일도 모두 제거한다.

1. make clean
2. make depmod
3. make mrproper
4. make distclean

```

## 문제 5번 : 정답 4

```

모듈의 의존성을 고려하여 모듈을 커널에 로드하는 명령은?

1. modinfo
2. insmod
3. rmmod
4. modprobe

```

## 문제 6번 : 정답 3

```

다음 중 리눅스에서 사운드 지원을 위한 대표적 오픈소스로 알맞은 것은?

1. CUPS, LPRng
2. LPRng, ALSA
3. OSS, ALSA
4. SANE, CUPS

```

## 문제 7번 : 정답 3

```

다음 중 모듈에 대한 설명으로 틀린 것은?

1. 커널 모듈은 필요에 따라 커널 이미지에 합류시키거나 해제할 수 있다.
2. 모듈 방식은 새 장치가 추가될 때마다 커널을 새롭게 컴파일할 필요가 없다.
3. insmod는 의존성을 고려하여 모듈을 자동 검색하고 삽입한다.
4. rmmod는 커널에서 모듈을 제거하며 사용 중인 모듈은 제거할 수 없다.

```

## 문제 8번 : 정답 2

```

다음 중 리눅스 시스템에 하드디스크를 추가하여 이용하는 과정에서 사용되는 명령으로 틀린 것은?

1. mount
2. backup
3. mkfs
4. fdisk

```

## 문제 9번 : 정답 1

```

다음 중 리눅스에서 사운드 카드용 장치에 대한 설명으로 틀린 것은?

1. alsamixer는 여러 사운드를 믹싱하는 GUI 프로그램이다.
2. ALSA는 사운드 카드를 자동 구성하고 다수의 사운드 장치 관리를 목적으로 한다.
3. OSS는 POSIX 시스템 콜과 호환되지 않는 독립적인 시스템 콜을 사용한다.
4. alsactl은 ALSA 사운드 카드를 초기화하거나 정보를 환경설정 파일에 저장할 수 있다.

```

## 문제 10번 : 정답 3

```

다음 중 SANE(Scanner Access Now Easy)에 대한 설명으로 틀린 것은?

1. 이미지 스캐너 및 카메라 하드웨어에 대한 표준화된 액세스를 제공한다.
2. 리눅스와 유닉스뿐 아니라 OS/2, 윈도우, macOS도 지원한다.
3. LGPL 라이선스이다.
4. SANE 패키지는 SANE 백엔드와 SANE 프론트엔드로 구성되어 있다.

```

## 문제 11번 : 정답 1

```

프린터를 USB 포트에 연결했을 때 생성되는 장치 파일명으로 알맞은 것은?

1. /dev/usb/lp0
2. /dev/sg0
3. /dev/lp0
4. /dev/sr0

```

## 문제 12번 : 정답 2

```

다음 중 /etc/passwd 파일을 2매 출력하려고 할 때 (　) 안에 들어갈 명령으로 알맞은 것은?

# (　) -# 2 /etc/passwd

1. lp
2. lpr
3. lpq
4. lpc

```

## 문제 13번 : 정답 4

```

다음 중 커널 컴파일 단계에서 메뉴 방식으로 커널 컴파일 옵션을 설정하는 명령으로 알맞은 것은?

1. make install
2. make bzImage
3. make mrproper
4. make menuconfig

```

## 문제 14번 : 정답 1

```

PCI 장치 정보를 목록 형태로 출력하는 명령은?

1. lspci
2. lsmod
3. lsblk
4. lscgroup

```

## 문제 15번 : 정답 2

```

특정 모듈을 제거하면서 의존성 있는 모듈을 같이 제거하려고 할 때 (　) 안에 들어갈 옵션으로 알맞은 것은?

# modprobe (　) ip6table_filter

1. -a
2. -r
3. -d
4. -e

```

## 문제 16번 : 정답 1

```

로컬 시스템에 직접 연결된 CUPS 프린터를 웹 브라우저로 접속하려고 할 때 알맞은 주소는?

1. http://localhost:631
2. http://localhost:2654
3. http://localhost:5900
4. http://localhost:8080

```

## 문제 17번 : 정답 4

```

다음 (　) 안에 들어갈 내용으로 알맞은 것은?

리눅스 커뮤니티에서는 사운드 카드용 장치 드라이버를 제공하기 위해 초기에는 (㉠)을 사용했으나, 라이선스 문제 등으로 현재는 (㉡)을 사용한다. (㉡)은 사운드 카드 하드웨어 자동 설정과 시스템상 복수 사운드 장치 지원을 목표로 시작되었다.

1. ㉠ ALSA, ㉡ OSS/Free
2. ㉠ SANE, ㉡ XSANE
3. ㉠ XSANE, ㉡ SANE
4. ㉠ OSS/Free, ㉡ ALSA

```

## 문제 18번 : 정답 1

```

다음 중 커널 이미지를 생성하는 명령으로 알맞은 것은?

1. make bzImage
2. make modules
3. make install
4. make help

```

## 문제 19번 : 정답 4

```

uname -r 결과가 2.6.32-696.el6.i686일 때 모듈 간 의존성을 기록한 파일의 경로로 알맞은 것은?

1. /etc/modprobe.d/2.6.32-696.el6.i686/modules.dep
2. /usr/src/kernels/2.6.32-696.el6.i686/modules.dep
3. /usr/local/src/2.6.32-696.el6.i686/modules.dep
4. /lib/modules/2.6.32-696.el6.i686/modules.dep

```

## 문제 20번 : 정답 3

```

다음 설명에 해당하는 명령으로 알맞은 것은?

System V 계열 프린터 명령으로 현재 설정된 프린터와 클래스, 인쇄 작업 상태를 출력한다. 인자를 생략하면 현재 사용자가 대기열에 추가한 활성 작업 정보를 출력한다.

1. lpc
2. lpinfo
3. lpstat
4. lpadmin

```
